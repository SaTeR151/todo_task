Postgres:
  ConnString: |
    host=localhost
    port=5432
    user=admin
    password=password
    dbname=todo_task
    pool_min_conns=1 
    pool_max_conns=10 
    pool_max_conn_idle_time=5m 
    pool_max_conn_lifetime=30m 
    pool_max_conn_lifetime_jitter=1m

Data:
  Password: TestPass